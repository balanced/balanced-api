{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "properties": {
        "amount": {
            "type": "integer",
            "minimum": 1,
            "required": true
        },
        "marketplace_fee": {
            "type": "integer",
            "minimum": 0,
        },
        "currency": {
            "type": "string",
            "enum": ["USD"],
        },
        "appears_on_statement_as": { "type": "string" },
        "description": { "type": "string", "required": true },
        "meta": { "type": "object" },
		"source": { "$ref": "cards.json" }
    }
}

POST /cards/:card_id/debits
require:
  - ../customer_fixtures.yml
scenarios:
  - name: fail_credit_customer
    request:
      method: POST
      href: "{customer,customers,credits}"
      schema:
        "$ref": "requests/customers/credit.json"
      body: {
        "amount": 1234
      }
    response:
      status_code: 409
      schema:
        "$ref": "responses/errors.json"
      assertRegex: { "description": "Customer does not have bank account associated" }
      matches: { "category_code": "funding-destination-not-associated" }
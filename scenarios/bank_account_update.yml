require:
  - bank_account_create.yml
scenarios:
  - name: bank_account_update
    request:
      href: "{bank_account_create,bank_accounts.href}"
      method: PUT
      body:
        meta:
          something: "ok testing"
    response:
      schema: |
        { "$ref": "../responses/bank_accounts.json" }
      equals: |
        { "bank_accounts": [ { "meta": { "something": "ok testing" } } ] }
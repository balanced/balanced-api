scenarios:
  - name: represent_dispute_without_evidence
    request:
      method: POST
      href:  "/disputes/{dispute,disputes.id}/represent"
      schema:
        "$ref": "requests/dispute.json"
      body: {}
    response:
      status_code: 400
      schema:
        "$ref": "responses/errors.json"
      assertRegex: { "description": ".*without attaching evidence" }

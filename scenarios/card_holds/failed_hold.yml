require:
  - ../card_fixtures.yml
scenarios:
  - name: create_failed_hold
    request:
      method: POST
      href: "{card_processor_failure,cards.card_holds}"
      schema:
        "$ref": "requests/cards/hold.json"
      body: {
        "amount": 444
      }
    response:
      status_code: 401

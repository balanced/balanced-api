require:
  - customer_credit.yml
scenarios:
  - name: create_reversal
    request:
      method: POST
      href: "{credit_customer,credits.reversals}"
      schema: |
        { "$ref": "../requests/credits/reversal.json" }
    response:
      status_code: 201
      schema: |
        { "$ref": "../responses/reversals.json" }